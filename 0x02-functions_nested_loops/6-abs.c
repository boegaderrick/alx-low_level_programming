#include "main.h"

/**
 * _abs - entry point
 *
 * Description: checks absolute value
 *
 * @i: number to be checked
 *
 * Return: 0
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
