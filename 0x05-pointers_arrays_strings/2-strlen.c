#include "main.h"
#include <string.h>

/**
* _strlen - checks string length
* @s: pointer to string to be checked
* Return: returns length of the string
*/

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
