#include "main.h"
#include <string.h>

/**
* _strcpy - copies strings
* @dest: string to be augmented
* @src: string to be copied
* Return: dest string
*/

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
