#ifndef MAIN_H
#define MAIN_H

/* HEADER FILES */
#include <stddef.h>

/* FUNCTION PROTOTYPES */
unsigned int binary_to_uint(const char *);
int _strlen(const char *b);
int _pow(int num, int power);

#endif /* MAIN_H */
