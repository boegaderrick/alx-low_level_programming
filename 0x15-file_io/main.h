#ifndef MAIN_H
#define MAIN_H

/* HEADER FILES */
#include <stddef.h>
#include <unistd.h>
#include <stdlib.h>
#include <fcntl.h>

/* PROTOTYPES */
int _putchar(char c);
ssize_t read_textfile(const char *filename, size_t letters);

#endif /* MAIN_H */
